.reset-pass-content
  %h4.text-center.heading.mt-10.mb-20 パスワード再設定
  = form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :put }) do |f|
    = f.hidden_field :reset_password_token
    .form-group
      = f.password_field :password, autofocus: true, autocomplete: "off", placeholder: "新しいパスワード", class: "form-control"
    .form-group
      = f.password_field :password_confirmation, autocomplete: "off", placeholder: "新しいパスワードを再入力", class: "form-control"
    .modal-footer
      .row.gap-10
        .col-xs-6.col-sm-6.mb-10
        = f.submit "パスワードを変更する", class: "btn btn-primary btn-block"
